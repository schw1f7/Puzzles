<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mythm.puzzles2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FWGC_intro"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Puzzle_intro"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Farmer_problem"
            android:parentActivityName=".FWGC_intro"/>
        <activity android:name=".Puzzle_problem"
            android:parentActivityName=".Puzzle_intro"></activity>
    </application>

</manifest>